// Specs: https://documentation.mjml.io/#mj-image
import type { Editor } from 'grapesjs';

export const type = 'vl-image';

export default class toggleImages {
  private editor: Editor;

  constructor(editor: Editor) {
    this.editor = editor;
    this.init();
  }

  private init() {
  
  }
};